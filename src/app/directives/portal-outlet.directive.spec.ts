import { PortalOutletDirective } from './portal-outlet.directive';

describe('PortalOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalOutletDirective();
    expect(directive).toBeTruthy();
  });
});
