import { SessionListFilterSearchCriteria } from './session-list-filter';

describe('SessionListFilter', () => {
  it('should create an instance', () => {
    expect(new SessionListFilterSearchCriteria()).toBeTruthy();
  });
});
