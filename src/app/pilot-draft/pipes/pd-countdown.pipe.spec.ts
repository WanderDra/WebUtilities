import { PdCountdownPipe } from './pd-countdown.pipe';

describe('PdCountdownPipe', () => {
  it('create an instance', () => {
    const pipe = new PdCountdownPipe();
    expect(pipe).toBeTruthy();
  });
});
