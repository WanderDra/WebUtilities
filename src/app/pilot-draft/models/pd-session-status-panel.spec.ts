import { PdSessionStatusPanel } from './pd-session-status-panel';

describe('PdSessionStatusPanel', () => {
  it('should create an instance', () => {
    expect(new PdSessionStatusPanel()).toBeTruthy();
  });
});
